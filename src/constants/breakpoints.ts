// media breakpoints in pixels
const breakpoints: { [key: string]: number } = {
  // extra small
  xs: 576,
  // small
  sm: 768,
  // medium
  md: 992,
  // large
  lg: 1200,
  // extra large
  xl: 1400,
};

export default breakpoints;
