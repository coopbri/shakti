---
index: 5
title: Text
description: Text component and props in the Shakti library.
path: /text
---

A <code>Text</code> component is an augmented <code>p</code> tag with the ability to mix in <DocLink to="/base">Base</DocLink> props as well as the below typography-oriented props:

<Remark>the below props are simpler overrides for the respective <code><DocLink to="/base#text">Base</DocLink></code> props; they are <i>shortcut</i> props. As such, you can use the corresponding base props instead, if you prefer.</Remark>

<PropTable propList={[...shaktiProps.shortcutText, ...shaktiProps.shortcutTextAlign]} />

Examples:

```tsx
<Text textCenter>Regular text!</Text>
<Text textCenter bold>Bold text!</Text>
<Text textCenter size={24} border="4px solid deeppink">size 24px text with a border!</Text>
```

```tsx render=true
render(() => (
    <>
        <Text textCenter>Regular text!</Text>
        <Text textCenter bold>Bold text!</Text>
        <Text textCenter size={24} border="4px solid deeppink">size 24px text with a border!</Text>
    </>
))
```